<h1>Coffees</h1>

<ul>
  <% @coffees.each do |coffee| %>
    <li>
      
      <%= link_to coffee.name, admin_coffee_path(coffee) %>
    </li>
  <% end %>
</ul>

<%= link_to "Create New coffee", new_admin_coffee_path %>